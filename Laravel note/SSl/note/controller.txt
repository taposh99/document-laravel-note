public function getModules($component_id)
{
    $modules = MasterModule::where('component_id', $component_id)
                ->where('status', 1)
                ->get(['id', 'name_en', 'name_bn']);

    return response()->json($modules);
}
