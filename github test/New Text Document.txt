hh




$ git add reset_lifecycle_file 

 
$ git status 

 
On branch main Changes to be committed: 

 
(use "git reset HEAD ..." to unstage) 

 
It Solution Stuff provide a collection of tutorials about PHP, Laravel Framework, Codeigniter Framework, Mysql Database, Bootstrap Front-end Framework, Jquery, Node JS, Ajax Example, APIs, CURL Example, Composer Packages Example, AngularJS etc. You will find the best example an article about PHP Language. So you can also search tutorial from bellow search box.
<tr>
    <td style="position: relative; padding-right: 10px;">
        <label style="display: block; margin-bottom: 5px;">Photo</label>
        <!-- File Upload Widget -->
        <?php
        $this->widget('ext.EAjaxUpload.EAjaxUpload', array(
            'id' => 'image',
            'config' => array(
                'action' => Yii::app()->createUrl('/plumber/plumber/upload'),
                'allowedExtensions' => array("jpg", "jpeg", "png", "gif"),
                'sizeLimit' => 2 * 1024 * 1024, // 2MB file size limit
                'multiple' => false,
                'onSubmit' => "js:function(file, extension) { 
                    $('div.preview').addClass('loading');
                }",
                'onComplete' => "js:function(file, response, responseJSON) {
                    if (responseJSON.success) {
                        $('#thumb').attr('src', '" . Yii::app()->request->baseUrl . "/uploads/plumberRegPhoto/' + responseJSON.filename);
                        $('#thumb').show();
                        $('div.preview').removeClass('loading');
                        $('#Plumber_photo').val(responseJSON.filename);
                        $('.qq-upload-list li:not(:last-child)').remove();
                    } else {
                        alert('Upload failed: ' + responseJSON.error);
                    }
                }",
                'messages' => array(
                    'typeError' => "{file} has invalid extension. Only {extensions} are allowed.",
                    'sizeError' => "{file} is too large, maximum file size is {sizeLimit}.",
                    'emptyError' => "{file} is empty, please select files again without it.",
                    'onLeave' => "The files are being uploaded, if you leave now the upload will be cancelled."
                ),
            )
        ));
        ?>
        <?php echo $form->hiddenField($model, 'photo'); ?>
        <!-- Image Preview -->
        <div style="margin-top: 10px;">
            <img id="thumb" src="" alt="Preview" style="display:none; width:100px; height:64px; border:1px solid #ccc;" />
        </div>
        <div id="fileName" style="margin-top: 10px;"></div>
        <?php echo $form->error($model, 'photo'); ?>
    </td>
    
    <td style="position: relative; padding-right: 10px;">
        <label style="display: block; margin-bottom: 5px;">NID Front Photo</label>
        <!-- File Upload Widget for NID Front Photo -->
        <?php
        $this->widget('ext.EAjaxUpload.EAjaxUpload', array(
            'id' => 'nid_photo_front',
            'config' => array(
                'action' => Yii::app()->createUrl('/plumber/plumber/upload3'),
                'allowedExtensions' => array("jpg", "jpeg", "png", "gif"),
                'sizeLimit' => 2 * 1024 * 1024,
                'onSubmit' => "js:function(file, extension) { $('div.preview').addClass('loading'); }",
                'onComplete' => "js:function(file, response, responseJSON) {
                    if (responseJSON.success) {
                        $('#nid_thumb_front').attr('src', '" . Yii::app()->request->baseUrl . "/uploads/plumberNid/' + responseJSON.filename);
                        $('#nid_thumb_front').show();
                        $('div.preview').removeClass('loading');
                        $('#Plumber_nid_photo_front').val(responseJSON.filename);
                        $('.qq-upload-list li:not(:last-child)').remove();
                    } else {
                        alert('Upload failed: ' + responseJSON.error);
                    }
                }",
                'messages' => array(
                    'typeError' => "{file} has invalid extension. Only {extensions} are allowed.",
                    'sizeError' => "{file} is too large, maximum file size is {sizeLimit}.",
                    'emptyError' => "{file} is empty, please select files again without it.",
                    'onLeave' => "The files are being uploaded, if you leave now the upload will be cancelled."
                ),
            )
        ));
        ?>
        <?php echo $form->hiddenField($model, 'nid_photo_front'); ?>
        <!-- Image Preview -->
        <div style="margin-top: 10px;">
            <img id="nid_thumb_front" src="" alt="NID Front Preview" style="display:none; width:100px; height:64px; border:1px solid #ccc;" />
        </div>
        <?php echo $form->error($model, 'nid_photo_front'); ?>
    </td>
    
    <td style="position: relative;">
        <label style="display: block; margin-bottom: 5px;">NID Back Photo</label>
        <!-- File Upload Widget for NID Back Photo -->
        <?php
        $this->widget('ext.EAjaxUpload.EAjaxUpload', array(
            'id' => 'nid_photo_back',
            'config' => array(
                'action' => Yii::app()->createUrl('/plumber/plumber/upload3'),
                'allowedExtensions' => array("jpg", "jpeg", "png", "gif"),
                'sizeLimit' => 2 * 1024 * 1024,
                'onSubmit' => "js:function(file, extension) { $('div.preview').addClass('loading'); }",
                'onComplete' => "js:function(file, response, responseJSON) {
                    if (responseJSON.success) {
                        $('#nid_thumb_back').attr('src', '" . Yii::app()->request->baseUrl . "/uploads/plumberNid/' + responseJSON.filename);
                        $('#nid_thumb_back').show();
                        $('div.preview').removeClass('loading');
                        $('#Plumber_nid_photo_back').val(responseJSON.filename);
                        $('.qq-upload-list li:not(:last-child)').remove();
                    } else {
                        alert('Upload failed: ' + responseJSON.error);
                    }
                }",
                'messages' => array(
                    'typeError' => "{file} has invalid extension. Only {extensions} are allowed.",
                    'sizeError' => "{file} is too large, maximum file size is {sizeLimit}.",
                    'emptyError' => "{file} is empty, please select files again without it.",
                    'onLeave' => "The files are being uploaded, if you leave now the upload will be cancelled."
                ),
            )
        ));
        ?>
        <?php echo $form->hiddenField($model, 'nid_photo_back'); ?>
        <!-- Image Preview -->
        <div style="margin-top: 10px;">
            <img id="nid_thumb_back" src="" alt="NID Back Preview" style="display:none; width:100px; height:64px; border:1px solid #ccc;" />
        </div>
        <?php echo $form->error($model, 'nid_photo_back'); ?>
    </td>
</tr>
