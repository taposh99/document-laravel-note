
new

ss
this is mnew and beryu beaytifuk
                'value' => 'Lookup::item("entry_source_type_id",$data->entry_source_type)',
this is new
hem plember installl 




 font-size: 14px !important;
 <td><label>Sales Person</label></td>

                <td>
                    <?php echo $form->hiddenField($model, 'territory_id', array('id' => 'SellItems_territory_id')); ?>
                    <?php
                    $this->widget('ext.select2.ESelect2', array(
                        'selector' => '#SellItems_territory_id',
                        'model' => $model,
                        'attribute' => 'territory_id',
                        'options' => array(
                            'allowClear' => true,
                            'multiple' => false,
                            'placeholder' => 'Select Sales Person',
                            'ajax' => array(
                                'url' => Yii::app()->createUrl('/supply_chain/territory/territoryListSelect2'),
                                'dataType' => 'json',
                                'quietMillis' => 100,
                                'data' => 'js: function(text,page) {
                    return {
                        q: text,
                        area_id: $("#SellItems_area_id").val(),
                        page_limit: 10,
                        page: page,
                    };
                }',
                                'results' => 'js:function(data,page) { var more = (page * 10) < data.total; return {results: data, more:more}; }',
                                'formatSelection' => 'js: function(data) { return data.title; }',
                            ),
                            'initSelection' => 'js: function(element, callback) {
                var territoryId = $(element).val();
                if (territoryId !== "") {
                    // Fetch the current value using the selected territory ID
                    $.ajax({
                        url: "' . Yii::app()->createUrl("/supply_chain/territory/territoryById") . '",
                        data: {id: territoryId},
                        dataType: "json",
                        success: function(data) {
                            callback({id: data.id, text: data.title});
                        }
                    });
                }
            }'
                        ),
                    ));
                    ?>
                </td>


